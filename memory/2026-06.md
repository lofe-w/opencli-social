# 2026-06 Memory

## 2026-06-02

- Created project as an OpenCLI plugin monorepo.
- Phase 1 scope is `publisher-weixin` for WeChat Official Account publishing through official APIs.
- Plugin command files must stay flat in each package root because OpenCLI scans plugin package directories directly.

